# My Projects — CLAUDE.md

## 📁 项目概览

这是统一工作区。所有工具、资料、项目都在此目录内。

## 🗂️ 目录结构

```
My Projects/
├── projects/              # 独立项目代码，每个子目录一个项目
│   ├── pdf2md/            PDF→Markdown 转换工具
│   ├── text-tools/        文本处理（神学精炼 refine_theology.py 等）
│   └── ocr-spell-check/   OCR纠错工具
├── books/                 # 书籍/纯文档（按专题分类）
│   ├── 伊斯兰在中国/       核心研究资料（85+册，含游记與世界史）
│   └── 亲爱的心灵/         亲爱的心灵系列（英译中+简繁双输出）
├── data/                  # 模型文件库（EasyOCR / Piper TTS 等）
├── audio/                 # TTS 音频输出
├── wiki/                  # 知识库（已按项目分类：系列/ 書庫/ 參考/ 概念/）
├── tools/                 # 系统辅助工具（g2pW、OCR校正管线等）
├── .venv/                 # Python 虚拟环境（统一使用，Python 3.10）
```

## 🐍 虚拟环境

```bash
# 不激活，直接使用完整路径
.venv/bin/python   <script>
.venv/bin/pip      install <package>
.venv/bin/edge-tts ...
```

## 🛠️ 最常用命令

### PDF→资料库（一步到位）
```
.venv/bin/python projects/pdf2md/pdf_to_library.py <书.pdf> --pages <范围> --seq <序号> [--books-dir "books/分类名"]
```

### 神学文本精炼（简繁双输出）
```
.venv/bin/python projects/text-tools/refine_theology.py <源文件.md> -o <输出前缀>
```

### 文字转语音
```
.venv/bin/edge-tts --voice zh-CN-YunjianNeural -f <文件.txt> --write-media audio/<输出>.mp3
```

### 播放音频
```
open -a "/Applications/OmniPlayerStore.app" audio/<文件>.mp3
```

### 知識庫（LLM Wiki）操作
```
# 查
/kb-query 桂花崗墓碑        # 查 wiki 中已編譯的知識
# 灌
/kb-ingest 白壽彝中國回教小史 # 灌注一本書到 wiki
# 掃
/kb-scan 元代色目人           # 掃書庫找相關史料
# 檢
/kb-lint                    # 檢查 wiki 完整性
```

## 📖 内容处理规则

处理「亲爱的心灵」系列及其他神学类书籍时，规则完整清单在 Memory（session-summary）中，包括但不限于：
- 先知ﷺ / 安拉ﷻ 尊称规范
- 圣门弟子ؓ / 先贤慈悯敬称规则
- 古兰经引用格式（原文中的安拉不加ﷻ）
- 繁简转换对照表
- 文学转写风格（去口语化）

**遇到拿不准的神学术语或格式，先问我，不要假设。**

## 🔄 工作方式

1. **以你的规则为先** — 你给过明确规则时（Memory 中、会话中指令），按规则执行。同一目标有多个方案时，**以最优实现为原则直接推荐一种**，简要说明理由；如有明显不同的替代方案再附带提及
2. **不确定就问，给出判断** — 神术语境、内容归属、意图模糊等拿不准的，先确认。同时给出**你认为最优的方案及理由**，让我快速决定。不要为了列全选项而过度变通
3. **手术刀式修改** — 只改任务要求的，不顺手"优化"不相干代码
4. **简单优先** — 不造抽象层，不写多余特性，50 行能解决的问题不用 200 行
5. **目标驱动** — 做之前说清做什么，做完自我验证
6. **有选项先推荐，不必列全** — 多个方案可行时，首选推荐你认为最优的一个，说明理由。替代方案仅在**有实质差异**时才附带列出。实在没有唯一最优解时才直接问"你倾向哪个"

## 💾 记忆管理

- 重要的新规则请对我说 **"记住这个"**，我会写入 Memory
- 会话中确认过的处理方式，后续同类任务自动沿用

## 🚫 对话纪律

1. **不说废话** — 不铺垫、不总结、不转述。直接回答、直接行动
2. **不捧用户** — 不说"好问题""说得好""厉害""正确"。客观陈述，不作评价
3. **纯净输出** — 不额外解释我正在做什么，除非你明确问。给结论而非过程
4. **告知而非征询** — 重大操作（不可逆覆写、破坏数据、影响系统安全）执行前一句话说明要做什么。不是请示，是告知
